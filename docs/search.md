# VJDownloader 動画高速検索エンジン

Rust + SQLite + notifyで、ローカルGUIアプリ向けの動画インデックス検索を提供します。
検索時にフルスキャンせず、事前インデックス + 差分更新で低レイテンシ検索を行います。

このファイルは**設計と実装ガイド**です。検索対象の拡張子、クエリの一致規則、正規化方式、
テーブル定義、UIの表示内容といった**振る舞いの仕様は`docs/spec.md`の「動画検索」**にまとめてあります。
仕様を変更するときは`docs/spec.md`を、設計判断や実装位置を変更するときはこのファイルを更新してください。

## 何ができるか
- 複数ルートフォルダ配下の動画ファイルを事前インデックス
- 日本語を含むファイル名・動画コメントの部分一致検索、空白区切りのAND検索
- メタデータ条件検索（`root` / `parent_dir` / `size` / `modified_time` / `limit` / `sort`）
- `notify`監視で追加・削除・リネームを差分反映
- GUI設定画面から検索対象フォルダの追加・削除、全再インデックス

対応する拡張子や一致規則の詳細は`docs/spec.md`を参照。

## 起動手順
```bash
cargo run
```

## テスト
```bash
cargo test search_index -- --test-threads=1
```

検索インデックスのテストは同一DBを触るため、`--test-threads=1`で直列実行する。

## 設計概要
- DB: SQLite（`~/.vjdownloader/search_index.sqlite3`）
- 書き込み: 単一ライタースレッド（キュー経由）
- 読み取り: 検索ワーカーでクエリ実行
- 初回/再構築: `walkdir`でフルスキャン
- 差分更新: `notify` + デバウンス

### SQLiteを使う理由
- ローカル埋め込みで配布が容易（単一ファイル）
- トランザクションと索引により高速かつ安全
- WALモードで読み取りと更新の並行性を確保しやすい

### 単一ライタースレッドにする理由
- SQLiteは同時書き込みを許さない。フルスキャン・差分更新・ルート同期がいずれも書き込むため、
  キューで直列化して呼び出し側からロック競合とリトライを消している

### 2段階検索の構成
- 単語1つのクエリは、第1段階でファイル名の前方一致のみを引き、完全一致を先頭に並べる
- 上限件数に届かない場合だけ、第2段階でファイル名とコメントの部分一致を引いて補う。
  第1段階で拾った前方一致はSQL側で除外しており、重複しない
- 実装は`src/search_index/mod.rs`の`search`と`src/search_index/query.rs`の`QueryPattern`

## 監視イベントの注意点
- OS監視イベントは順序入れ替わり・取りこぼしが起こり得ます
- 本実装はデバウンスとイベント統合で吸収し、監視エラー時は有効ルートを再スキャンします
- それでもDBが実体とずれた場合は、設定画面の`全体を再インデックス`で復旧できます

## 大規模フォルダ運用の注意
- 初回スキャン時間はファイル数に比例して増加します
- ルートを絞るほどDBサイズと更新負荷を抑えられます
- 不要ディレクトリは検索対象ルートに含めない運用を推奨

## 既知の制限
- 意味的同義語検索（例: 表記ゆれ辞書）やかな漢字変換は未対応
- OS依存のパス表現差（シンボリックリンク経由など）は完全吸収していない

## 将来拡張
- 動画メタ情報（長さ・解像度）の追加カラム
- タグ/お気に入りなどのユーザー属性
- ランキング強化（前方一致・最近利用・ルート重み）

## 実装定数

いずれも`src/search_index/`内で定義しており、値を変えるときはここを直接編集する。
仕様値として`docs/spec.md`にも記載があるものは両方を更新する。

| 定数 / 関数 | 既定値 | 意味 |
| --- | --- | --- |
| `normalize_for_search` | NFKC + lower + カタカナのひらがな化 | 検索正規化方式 |
| `DEBOUNCE_WINDOW` | 700ms | 監視イベントのデバウンス幅 |
| `UPSERT_BATCH_SIZE` | 256 | バッチupsert件数 |
| `MAX_SEARCH_LIMIT` | 1000 | 1回の検索で返す最大件数 |

## 実装主要ファイル

リポジトリルートからの相対パスで記載する。

- `src/search_index/mod.rs`: インデックス本体（ワーカー起動・検索・再インデックス制御）
- `src/search_index/db.rs`: SQLiteスキーマとクエリ実行
- `src/search_index/normalize.rs`: NFKC + lower + ひらがな化の正規化
- `src/search_index/query.rs`: 検索クエリの解析
- `src/search_index/scanner.rs`: `walkdir`によるフルスキャン
- `src/search_index/watcher.rs`: `notify`監視とデバウンス
- `src/search_index/writer.rs`: 単一ライタースレッド
- `src/settings/ui.rs`: 検索対象フォルダの追加・削除、全体を再インデックスのUI
- `src/settings/mod.rs`: 設定ファイルの読み書き
- `src/paths.rs`: DBファイルなど内部パスの解決
- `src/app.rs`: アプリ本体との結線
